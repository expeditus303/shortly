q# shortly
"Shortly" is a Node.js and PostgreSQL-based backend application for a URL shortener website. It efficiently generates short URLs and stores them in a database, making it easier to share and access long links.
